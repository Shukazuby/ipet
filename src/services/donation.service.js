const httpStatus = require('http-status');
const ApiError = require('../utils/ApiError');
const { db } = require('../models');

const donatePet = async(userId, petId, donateBody)=>{

}

module.exports ={
    donatePet
}